resources:
- allow-argocd-to-manage.yaml
- chains-operator-cr.yaml
- chains-secrets-config.yaml
- chains-config.yaml

patchesStrategicMerge:
#
# Add some chains configuration
- chains-config.yaml
#
# Create the custom ca configmap. This must exists prior to patch the cluster proxy.
- chains-ca-bundle.yaml

# Add the custom ca to the cluster proxy.
- proxy-cluster.yaml

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
