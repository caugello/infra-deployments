resources:
- allow-argocd-to-manage.yaml
- chains-operator-cr.yaml
- chains-admin.yaml
- chains-config.yaml

patchesStrategicMerge:
#
# Add some chains configuration
- chains-config.yaml

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
